# x1EdgeAberration

`x1EdgeAberration` applies chromatic edge splitting only where image structure and lens-edge weighting support it.

Use it for:

- comp finishing
- lens imperfection passes
- sci-fi or stylized edge separation

Outputs:

- processed `IMAGE`
- aberration `MASK`
- info `STRING`
