# MKRShift After Effects Plugin

This is the install-facing After Effects ScriptUI bridge package.

Copy the JSX file into your After Effects ScriptUI Panels or scripts folder, depending on how you want to load it.

The bridge now supports:

- render, image, and playback spec building
- endpoint header generation
- conservative socket-based submit/poll helpers for endpoint roundtrip workflows
