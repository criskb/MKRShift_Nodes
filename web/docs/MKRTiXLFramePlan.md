# MKRTiXLFramePlan

`MKRTiXLFramePlan` builds a return packet for TiXL / Tooll layer ingestion.

Typical uses:

- file-based texture/video exchange
- NDI / Spout / OSC handoff plans
- layer naming and blend-mode manifests

Outputs:

- `tixl_frame_plan_json`
- `manifest_line`
- `summary_json`
