# MKRShift Plugin Endpoint Helpers

Shared helpers for legacy host plugins under `plugins/`.

These mirror the newer addon endpoint contract so older TouchDesigner and TiXL bridge experiments can still participate in:

- endpoint-plan driven submits
- transport-plan driven routing
- stable packet/result handoff
